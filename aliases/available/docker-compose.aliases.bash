cite 'about-alias'
about-alias 'docker-compose abbreviations'

alias dco="docker-compose"
alias dcofresh="docker-compose stop ; docker-compose rm -f --all ; docker-compose up -d ; docker-compose logs -f"
